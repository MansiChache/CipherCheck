{
    "name": "qr-attendance",
    "version": "1.0.0",
    "main": "",
    "homepage": ".",
    "scripts": {
        "start": "npm start --prefix server",
        "install-client": "cd client && npm install && npm run build && cd ..",
        "install-server": "cd server && npm install && cd .. ",
        "op": "npm run install-client && npm run install-server"
    }
}